# 쿼카 AI 개념극장 — 이미지 요청 프롬프트 세트

GPT(이미지 생성)에 아래 프롬프트를 그대로 주고, 결과를 지정된 파일명으로 저장해 `assets/char/`, `assets/key/`에 넣은 뒤 `assets/manifest.js`에 이름을 한 줄 추가하면 사이트에 반영돼요. 없으면 기본 스티커(`idea.webp`, `globe.webp`)로 대신 나와요.

## 공통 규칙 (모든 프롬프트 앞에 붙이기)

> 참고 이미지의 쿼카 캐릭터(경기 쿼카연구회 오리지널)와 **완전히 같은 캐릭터**로 그려 주세요. 얼굴·비율·색을 바꾸지 마세요. 갈색 등, 연한 황갈색 몸, 흰 배, 점 눈, 작은 미소, 흰색 스티커 테두리. 유명 캐릭터를 닮게 만들지 마세요. 배경은 **투명(PNG, 알파)**. 글자·로고 없음. 플랫 벡터 스티커 스타일, 그라데이션·네온·3D 광택 없음. 정사각형 1024×1024.

참고 이미지: `보라 전구를 든 쿼카.png`(스티커 원본)를 함께 첨부해 주세요.

## A. 해설자 포즈 8종 → `assets/char/{파일명}.png` (webp로 바꿔도 돼요)

| 파일명 | 프롬프트(공통 규칙 뒤에 이어 붙이기) | 쓰이는 곳 |
|---|---|---|
| `base` | 정면, 두 팔을 편하게 내리고 살짝 미소. 소품 없음. | 기본 해설자 |
| `point` | 정면, 오른팔을 들어 화면 오른쪽 위를 가리키는 포즈. 눈은 가리키는 쪽을 봄. | 도식 설명 |
| `think` | 한 손을 턱에 대고 고개를 살짝 기울인 생각하는 포즈. 눈썹 살짝 올림. | 질문·오해 장면 |
| `oops` | 두 손으로 볼을 감싸고 놀란 표정(입 동그랗게). 머리 위에 작은 물음표 없이. | 오류·뭉개진 글자 장면 |
| `tablet` | 두 손으로 태블릿을 들고 화면을 보는 포즈. 태블릿 화면은 흰색 빈 화면. | 에이전트·도구 장면 |
| `wave` | 한 손을 들어 인사하는 포즈, 밝은 미소. | 오프닝·엔드카드 |
| `dice` | 한 손에 흰색 주사위 하나를 들고 살짝 던지려는 포즈. | E2 확률·시드 |
| `glass` | 한 손에 돋보기를 들고 들여다보는 포즈. | E4 가짜 단서 찾기 |

## B. 에피소드 키비주얼 6장 → `assets/key/{slug}.png` (16:9, 1536×864, 배경 아주 연한 회백색 `#FAFBFC`)

공통: 같은 쿼카 캐릭터가 작게 한 마리 등장, 나머지는 단순한 도형으로 개념을 표현. 포인트 색은 아쿠아블루 `#2BB3C9`와 오렌지 `#F2812D` 둘만. 글자 없음. 여백 넉넉하게.

| 파일명 | 프롬프트 |
|---|---|
| `e1-agent-mcp` | 쿼카가 태블릿을 들고 있고, 태블릿에서 나온 선이 커서·키보드·눈 아이콘 세 개의 둥근 상자로 이어지는 고리. 상자들은 하나의 둥근 소켓(USB-C처럼 생긴 단순 플러그 모양)에 꽂혀 있음. |
| `e2-seed` | 쿼카가 주사위를 던지고, 주사위에서 세 갈래로 갈라진 길 끝에 비슷하지만 조금씩 다른 세 개의 작은 풍경 카드(해변·언덕·마을). |
| `e3-diffusion` | 왼쪽은 회색 모래알 같은 잡음 사각형, 오른쪽으로 갈수록 점점 또렷해져 마지막엔 쿼카 얼굴이 되는 5단계 사각형 연속. |
| `e4-plausible` | 쿼카가 돋보기로 오렌지빛 계기판 화면을 들여다봄. 계기판 숫자 자리는 뭉개진 흐릿한 막대 모양(실제 숫자 없이). |
| `e5-context` | 쿼카가 작은 책상 앞에 앉아 있고, 책상 위에 색색의 종이 조각(토큰 칩)이 가득. 책상 밖으로 종이 몇 장이 떨어지는 중. |
| `e6-rag` | 쿼카가 서류 뭉치를 안고 있고, 서류에서 아쿠아색 선이 나와 말풍선에 꽂힘. 말풍선 아래에 작은 각주 표시(점 세 개). |

## C. 첫 화면·공유용 2장

| 파일명 | 규격 | 프롬프트 |
|---|---|---|
| `assets/key/hero.png` | 16:9, 1536×864, 배경 `#FAFBFC` | 쿼카가 작은 빨간 극장 의자에 앉아 줄무늬 팝콘 통을 들고 화면을 올려다봄. 오른쪽 절반은 비워 둠(제목이 들어갈 자리). 아쿠아·오렌지 포인트만. |
| `assets/og.png` | 1200×630, 배경 `#FAFBFC` | 위 hero와 같은 구도를 1200×630에 맞게. 글자 없음. |

## D. 다음 시즌용(나중에) → `assets/key/next-{n}.png`

1. 얼굴이 조금씩 다른 쿼카 4마리가 나란히, 첫 번째만 사진 액자 안에 있음(레퍼런스 이미지).
2. 이미지 한 장 옆에 같은 이미지가 24장 쌓인 필름 띠(영상은 여러 장).
3. 두 개의 말풍선, 하나는 크고 하나는 작음, 큰 쪽에서 더 많은 선이 카메라로 이어짐(언어 비중).
4. 쿼카가 모래시계를 들고 기다리는 포즈(추론 시간).
5. 말풍선 → 코드 블록 → 고양이 모양 구름(깃허브 느낌 없이) → 화면, 네 단계 흐름(바이브코딩).
6. 그림 모서리에 작은 라벨 태그와 투명한 물결무늬(워터마크).

## 저장 후 할 일

1. PNG를 webp로 바꾸면 더 가벼워요(선택). 파일명은 확장자만 다르게.
2. `assets/char/`와 `assets/key/`에 넣고, `assets/manifest.js`에 `이름: 'png'`처럼 한 줄 추가해요(예: `char: { idea:'webp', base:'png', point:'png' }`, `key: { hero:'png' }`).
3. CREDITS.md의 'AI 생성 자산' 표에 파일명과 사용 도구를 한 줄씩 적어 주세요.
